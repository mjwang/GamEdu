#GamEdu
EC.717 D-Lab:Education
Team Colombia

#Setup

* Install node.js
* run `npm install` in the base directory.
* run `node index.js` in the base directory.
* Go to http://localhost:3000 to start the server.
